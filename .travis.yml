language: c
sudo: required
branches:
  only:
    - "master"
    - "develop"

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y ola

before_script:
  - mkdir build
  - cd build
  - cmake ..

script: make && ctest -V
